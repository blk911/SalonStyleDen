Understanding the problem
The error "Rendered more hooks than during the previous render" in a React Error Boundary typically arises when the number of hooks called within a functional component changes between renders. React needs the number and order of hooks to remain consistent across renders to maintain component state correctly. This issue often occurs due to conditional hook calls or hooks within loops, leading to an inconsistent hook execution path.

In your case, the error originates in the NetworkVisualization component at line 443, which calls useEffect. The surrounding code you've provided doesn't directly show the issue within NetworkVisualization, but it suggests that console overriding or global error handling might be indirectly affecting the component's rendering behavior, possibly by triggering re-renders or altering the execution flow in a way that impacts hook calls within NetworkVisualization.

How to verify
Inspect NetworkVisualization: Examine the NetworkVisualization component, especially around line 443, to identify any conditional useEffect calls or hooks within loops. Ensure that the hooks are always called in the same order and quantity during each render, regardless of the component's state or props.
Check component re-renders: Use React DevTools or add console logs within NetworkVisualization to monitor how often it re-renders and what state/prop changes trigger these re-renders. Investigate if the re-render frequency or the data passed to the component could be causing inconsistent hook calls.
Isolate console/error handling: Temporarily disable the overrideConsole, catchGlobalErr, and potentially setGlobal calls within your code to see if the error disappears. This will help determine if the issue is related to the console manipulation or global error handling interfering with React's rendering process.
Suggested fix
Without the code for NetworkVisualization, the exact fix is difficult to determine. However, here are some general strategies based on the common causes:

Ensure consistent hook calls: Modify the logic within NetworkVisualization to ensure that all hooks, including useEffect at line 443, are called unconditionally and in the same order during every render. If hooks are within conditional statements or loops, refactor the code to ensure consistent execution paths. For example, move conditional logic inside the hook rather than conditionally calling the hook itself.
Code

// Instead of:
   if (someCondition) {
     useEffect(() => { ... }, []);
   }

   // Do:
   useEffect(() => {
     if (someCondition) {
       // ... your effect logic ...
     }
   }, [someCondition]); // Ensure all dependencies are included
Use code snippets with caution

Review console and error handling interactions: If disabling console overriding or global error handling resolves the issue, carefully review how these features interact with NetworkVisualization and its rendering cycle. It's possible that console messages or intercepted errors are triggering unintended re-renders or modifying the component's state in a way that leads to inconsistent hook calls. Consider debouncing or throttling any actions triggered by console messages or errors, and ensure that state updates are batched appropriately.
Error boundary placement: While you already have an error boundary catching the error, ensure it's placed correctly to isolate the NetworkVisualization component and prevent the error from propagating further up the component tree.
Summary
The "Rendered more hooks than during the previous render" error suggests inconsistent hook usage within the NetworkVisualization component. Verify that all hooks are called unconditionally and in the same order during each render. If console overriding or global error handling is affecting component behavior, carefully review their interactions with NetworkVisualization and consider isolating or modifying their behavior to prevent interference with React's rendering cycle.

